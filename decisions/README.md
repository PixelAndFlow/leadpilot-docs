# decisions/

A log of every significant decision made during LeadPilot's build.
Each entry records what was decided, why, and what alternatives were
considered.

## What belongs here

- All decisions from the log below
- New decisions added during the build
- Decisions that were reversed or changed, with reason why

## Suggested file naming

  decisions-log.md      Single running log of all decisions

## Entry format

  ## Decision [number] — [short title]
  Date: [date]
  Decision: [what was decided]
  Reason: [why this was chosen]
  Alternatives considered: [what else was evaluated and why rejected]
  Status: Active / Superseded by Decision [number]

## Decisions log (current)

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 001 | Agent name | LeadPilot | Reflects the "autonomous co-pilot for outbound sales" positioning |
| 002 | Ownership | Marc Delsoin, Abdoul Ba (co-owners) | Per PRD v1, July 6, 2026 |
| 003 | Docs repo structure | Adopted the NoiseToSignal docs schema, expanded (governance/, roadmap/, release-process/, monitoring-observability/, public/) | Reuse proven structure; add professional-repo elements identified as gaps before LeadPilot's build starts |
| 004 | Repo separation | Two private repos: `leadpilot` (code) and `leadpilot-docs` (planning/security/decisions) | Security separation and independent access control — see leadpilot-docs/README.md |
| 005 | Tech stack | ~~Deferred~~ — **Superseded by Decision 022** | Not yet decided; expected to change substantially — see tech-stack/README.md |
| 006 | Prompt injection defense | Isolated validation layer strips instruction-like keywords from all spreadsheet-sourced text before tool execution | Per PRD 3c blast radius — indirect prompt injection is the identified worst-case failure mode |
| 007 | Duplicate contact prevention | Atomic state locking — run timestamps committed before tool calls are authorized | Per PRD 3c — timing-gap duplicate contact is a named failure mode with brand-damage impact |
| 008 | File completeness validation | Require file size >5KB and strict PDF extension match before counting a document as present | Per PRD 3c — prevents false-positive Slack handoffs on empty/invalid files |
| 009 | Outreach/handoff auto-send vs. rep approval | Nothing fires automatically — the agent only drafts (outreach, back-office handoff, spreadsheet edits); every side-effect action requires explicit rep approval via a single-use approval token | Resolves the Phase 1 open decision below. Marc/Abdoul decided the rep must stay in control of what actually sends, calls, or writes — see PRD v1.01 section 2 and the "changed to make" notes |
| 010 | Back-office handoff channel | The handoff can be either a phone call (`initiate_backoffice_call`) or a predetermined Slack message (`dispatch_slack_handoff`) to the same 3 stakeholders — both staged, both require rep approval | Per PRD v1.01 section 3b — expands the handoff beyond Slack-only in PRD v1 |
| 011 | Spreadsheet write-back | Added `update_lead_sheet` tool — reps edit lead data from the unified interface instead of the source sheets directly; writes only fire after the rep confirms a shown current-vs-proposed diff | Per PRD v1.01 1b/3a — removes the last reason a rep would need to leave the unified interface |
| 012 | Communications search | Added `search_communications` tool — searches a client's email/text history by any known identifier (email, phone, full name, company name), returning history, attachments, and confirmed documents | Per PRD v1.01 2a — a client is often reachable through more than one contact point; the rep needs history regardless of which one was used |
| 013 | Access control | Only authenticated, pre-authorized rep accounts may trigger an agent run, view lead/contact data, or approve a staged action | Per PRD v1.01 2c/3c — "lock down use of the agent by either the account using the agent or some other method" from the changed-to-make notes; resolved as an authenticated-session requirement |
| 014 | Lead outreach execution model | Added `initiate_lead_call`, `send_lead_text`, `send_lead_email` — all stage drafts only, same rep-approval gate as every other side-effect tool, no autonomous send | Closes a gap in v1.01: lead outreach (call/text/email) was required by 1b, section 2, the system prompt, and Eval Case 1, but no tool existed to back it. Marc confirmed outreach follows the same approval gate as everything else — no carve-out — see PRD v1.02 section 3a |
| 015 | Lead-source connector architecture | Introduced a `LeadSourceConnector` interface (`list_sources`, `fetch_rows`, `write_field`, `detect_changes`); only `GoogleSheetsConnector` is built now, behind `fetch_all_leads`/`update_lead_sheet` | Keeps prioritization/next-best-action logic from hard-coding Sheets-specific assumptions, so later sources don't require reworking the tools that consume them — see PRD v1.02 section 3e |
| 016 | Lead call handoff mechanism | `initiate_lead_call` no longer calls any telephony API. On rep approval, LeadPilot copies the lead's phone number to the clipboard and shows a confirmation message; the rep places the call manually in whatever tool they use | Google Voice has no official public API (confirmed), and its Acceptable Use Policy explicitly prohibits scripting or automating the placement of calls/messages, including stopping short of the final dial — so pre-filling Voice's dialer via browser automation is still a policy violation, not just a fragility risk. Clipboard handoff never touches Voice or any calling app, so neither problem applies — see PRD v1.03 section 3a and `testing/known-issues-log.md` Issue 001 |
| 017 | Equity split and decision rights | Equal 50/50 ownership between Marc and Abdoul; either partner may fork or use the work as they see fit | Confirmed directly by Marc, 2026-07-08 — documented in governance/README.md. Still flagged there as needing a real written agreement, since "use it as they see fit" doesn't on its own resolve naming/data/liability questions if one partner forks solo |
| 018 | Contact history data source | `get_contact_history` reads from a LeadPilot-owned, append-only contact-history log instead of any Google Voice API | Google Voice has no API to poll (Issue 001); LeadPilot already knows about every action it stages/sends, so it can log its own history rather than depending on an external call-log service. See architecture/state-schema.md and PRD v1.04 section 3f. Call-outcome gap closed by Decision 020 (`log_call_outcome`) |
| 019 | Back-office handoff channel (supersedes Decision 010) | `initiate_backoffice_call` is retired. Back-office handoffs are always a Slack message via `dispatch_slack_handoff`, including a new `urgent_callback_request` message type for what used to be the call option — still rep-approved, no autonomous-send exception | Closes Issue 001 entirely — no tool depends on a Google Voice API anymore. Confirmed directly with Marc that urgency is not a reason to skip rep approval, even for an internal-only recipient — see PRD v1.04 section 3a/3b and Eval Case 9 |
| 020 | Call-outcome logging | Added `log_call_outcome` — a rep-initiated tool (not agent-drafted, no approval token needed) that records answered/no-answer/voicemail/didn't-call against a call's contact-history log entry | Closes the gap flagged in Decision 018: since calls happen entirely outside LeadPilot (Decision 016), outcomes aren't automatically knowable. Without this, Rank 3's unanswered-call follow-up rule has no data for calls specifically. See PRD v1.04 section 3a/3f and Eval Case 10 |
| 021 | Rep-approval enforcement mechanism (resolves Issue 003) | No separate token object. The contact-history log row (Decision 018) carries a `stage` field (`drafted -> awaiting_rep_approval -> approved -> executed`/`rejected`/`expired`); approval flips it to `approved`, and execution does a single atomic conditional update (`UPDATE ... SET stage='executed' WHERE stage='approved'`), checking exactly one row was affected before firing the real effect | Simplest correct mechanism — the same row that's already the audit trail (Decision 018) is also the enforcement point, so there's no second system to build or keep in sync. Pairs with the authenticated-session requirement (Decision 013): session confirms who approved, this confirms the action fires exactly once. See architecture/state-schema.md. Alternatives considered: a signed JWT-style token (rejected — the token never leaves the backend, so cryptographic signing adds complexity without adding real security over a database check); an opaque token in a separate table (rejected — functionally redundant with the state machine, would duplicate bookkeeping). **Update 2026-07-10:** the concurrency test flagged below as unwritten now exists — `leadpilot/tests/test_gate.py::test_try_execute_is_single_use_under_concurrency` fires 10 real simultaneous execute attempts at the same approved row against real Postgres; exactly one wins, every time |
| 022 | Tech stack (supersedes Decision 005) | Python + Claude Agent SDK (agent runtime); FastAPI + Jinja2/htmx (dashboard, no separate SPA); Postgres via Neon (state store — not SQLite); Render Cron Job (hourly batch run) + Render Web Service (always-on dashboard), sharing the same Postgres instance; `google-api-python-client`/`google-auth-oauthlib` (Sheets/Drive/Gmail), Twilio SDK (SMS), `slack_sdk` (Slack); GitHub Actions running the eval suite per PR | Python for integration-library maturity (Google/Twilio/Slack) and full control over the tool-calling loop, given how much of the PRD depends on knowing exactly what the agent did (security guard, exact eval-card matching) — rejected LangChain (unneeded abstraction) and an orchestration platform (less control over the security layer). Postgres over SQLite specifically because the Web Service and Cron Job are separate containers — a local SQLite file isn't visible across both, so a networked DB is needed from day one, not after. Gmail API over a separate email vendor to keep outbound email inside the Workspace ecosystem this product already runs in and feed `search_communications` naturally. Full writeup: tech-stack/stack-overview.md |
| 023 | Rep-session identity mechanism (resolves Decision 013's open item) | Email + password, with our own DB-backed sessions (`leadpilot/models/rep.py`, `leadpilot/auth.py`) — not Google OAuth | Confirmed directly with Abdoul, 2026-07-09, while building Step 1. Keeps rep login independent of Step 0's Google Cloud project (Abdoul was building Step 1 before Marc had started Step 0), and a DB-backed session can be revoked immediately — logout, or deactivating a rep — instead of waiting on token expiry. Session tokens are itsdangerous-signed as a tamper-integrity layer, but revocability/expiry enforcement is the DB row, same "database is the source of truth" approach as Decision 021. Alternatives considered: Google OAuth (rejected for now — re-couples rep login to Step 0's account creation; reconsider if reps end up needing SSO for other reasons) |
| 024 | ~~Google Sheets credential type for `fetch_all_leads`/`update_lead_sheet`~~ — **Superseded by Decision 026** | A Google service account (`GOOGLE_SERVICE_ACCOUNT_KEY_PATH`), not the `GOOGLE_OAUTH_CLIENT_ID`/`SECRET` OAuth-consent flow `commands/README.md` originally assumed for all Google access | `fetch_all_leads`/`update_lead_sheet` run inside an unattended hourly Cron Job — there's no human present to click through an OAuth consent screen, and no per-rep identity is relevant to reading/writing a shared business spreadsheet. A service account authenticates directly with no consent flow or refresh-token storage needed. `GOOGLE_OAUTH_CLIENT_ID`/`SECRET` are kept in `.env.example` for Gmail-as-the-rep (Step 2, `send_lead_email`), which is a genuinely different, per-rep-consent case — sending as a specific rep's own Gmail account can't go through a service account the same way. **Confirmed by Marc, 2026-07-11 (Step 0):** OAuth would only have bought per-rep attribution inside Google Sheets' own revision history, and `update_lead_sheet` writes are already attributed to the approving rep at the LeadPilot layer via the contact-history/approval-gate log (Decision 013), independent of which Google credential makes the API call. Against that thin benefit, OAuth would add a consent flow and long-lived refresh-token storage/expiry handling that this unattended Cron Job has no natural way to satisfy — a service account is the standard, textbook credential for one app with unattended access to one org's shared resources, which is exactly Phase 1's single-org scope. Status: Active. Marc also agreed Drive should follow the same service-account pattern once `verify_drive_contents` is built (Step 2) rather than defaulting back to OAuth — see the Open decisions note below, still needs a dedicated decision entry when that connector is actually built. **Superseded 2026-07-11 (Decision 026):** Marc reversed this after a Step 0 discussion about on-demand sheet access — see Decision 026 |
| 025 | Dedup/run-lock table schema (resolves the open item below) — **`agent_run_locks`' singleton-mutex design partially superseded, see Decision 027** | Three tables, not one: `lead_source_rows` (maps raw per-sheet rows to a canonical `leads.lead_id`, structural only — the matching heuristic itself is Step 2 logic), `lead_action_locks` (per-lead duplicate-contact prevention, one row per lead, atomically UPSERTed), and `agent_run_locks` (a singleton mutex so the hourly Cron Job can't run twice concurrently, with a staleness fallback so a crashed run doesn't block every future run forever) | "Run-lock" in the build order turned out to name two genuinely different failure modes from security/threat-model.md (a lead getting double-contacted vs. the batch job itself double-running), so one table wasn't enough. Both lock types use the same atomic `INSERT ... ON CONFLICT DO UPDATE ... WHERE` pattern (one round trip, no racy SELECT-then-UPDATE) and are proven race-safe under real concurrent load in `leadpilot/tests/test_locks.py` — this is the literal scenario security/pen-test-checklist.md names under "Duplicate-contact / race condition checks." See architecture/state-schema.md (to be updated) and `leadpilot/models/dedup.py`/`leadpilot/models/run_lock.py`/`leadpilot/locks.py` in the code repo |
| 026 | Google Sheets/Drive access model (supersedes Decision 024) | Per-rep OAuth using the `drive.file` scope plus the Google Picker API — not a service account, and not the broad `spreadsheets`/`drive` OAuth scopes. Each rep does a one-time "Connect Google Account" consent (after logging in via Decision 013's session system); LeadPilot stores that rep's refresh token and uses it for both on-demand sheet lookups and the hourly batch run acting on that rep's behalf. Extends to `verify_drive_contents` as well as `fetch_all_leads`/`update_lead_sheet` | Reversed directly by Marc, 2026-07-11, during a Step 0 discussion: (1) sheets must never be pre-shared to a standing service-account identity — the rep's own Google permissions should be what decides whether LeadPilot can touch a given sheet, so an agent that needs to jump to a new sheet a rep hands it "without notice" can't rely on a pre-shared allowlist; (2) a single service account made every write look identical regardless of which rep triggered it — Marc wants activity trackable per rep, which the service account model couldn't give at the Google layer no matter what LeadPilot's own internal contact-history log shows. `drive.file` was chosen over the broader `spreadsheets`/`drive` OAuth scopes specifically because it only grants access to files the rep explicitly selects via Google's Picker widget — matching "sheets are shared to the agent, deliberately, one file at a time" far more precisely than a blanket "read everything this rep can see" grant, and it avoids Google's sensitive-scope app-verification review that broader scopes can trigger. Alternatives considered: keeping the service account for the batch job and adding per-rep OAuth only for on-demand lookups (rejected — Marc wants one consistent access model, not two, and the activity-tracking goal applies to the batch job's writes just as much as ad hoc ones); domain-wide delegation (not seriously considered — a Workspace-admin-level standing grant to impersonate any user is a bigger blast radius than what this problem needs, and reintroduces the "one privileged identity" issue Marc is trying to move away from). **Update 2026-07-11:** documentation for this decision (and 027-028 below) merged to `main` in both repos — `leadpilot-docs` PR #1 (commit `8e902af`), `leadpilot` (commit `17fbc3e`) — reviewed and approved by Abdoul |
| 027 | Batch run execution model (updates Decision 022, partially supersedes Decision 025) | The hourly `fetch_all_leads` run becomes per-rep, not a single global run: the Cron Job iterates over every rep who has connected a Google account (Decision 026), running the fetch/prioritize/verify/draft sequence once per rep using that rep's own OAuth grant, so each rep's queue only ever contains leads from sheets they personally connected. `agent_run_locks` (Decision 025) needs to change from a singleton mutex to a per-rep mutex — exact schema change is Step 2 work, flagged here rather than designed now | Direct consequence of Decision 026: once sheet access is scoped to what an individual rep connected, there is no longer one global list of "designated spreadsheets" for a single run to process — the unit of work is now (rep, their connected sheets), not (the org, its sheets). Keeps the same Render Cron Job (Decision 022) rather than introducing a new scheduling primitive; only the loop body changes |
| 028 | New tool: on-demand ad hoc sheet lookup | Provisionally named `fetch_ad_hoc_sheet` (name/signature TBD with Abdoul in Step 2) — lets a rep point LeadPilot at a specific sheet mid-session (via the same `drive.file`/Picker consent as Decision 026) for a one-off read, outside the routine hourly `fetch_all_leads` scan. Read-only for now; no rep-approval gate needed since it has no external side effect, same reasoning as `fetch_all_leads`/`get_contact_history` | Closes the gap identified in the Step 0 discussion 2026-07-11: none of the PRD v1.04 tools support a rep saying "check this sheet right now" — `fetch_all_leads` only scans sheets already connected ahead of time. Needs a PRD v1.05 tool-table entry, a system-prompt step, and an eval case (see testing/eval-suite.md Case 11) before Step 2 builds it |
| 029 | Encryption approach for stored Google refresh tokens (`rep_google_credentials`, Decision 026) | Application-level symmetric encryption (Fernet, via Python's `cryptography` library), keyed by a single `CREDENTIAL_ENCRYPTION_KEY` env var — not Postgres's `pgcrypto` extension, and not a cloud KMS. `leadpilot/crypto.py` encrypts before insert and decrypts on read; the database only ever stores ciphertext | Matches Decision 021's "simplest correct mechanism" precedent — Phase 1 is a single unattended service with one org's worth of reps, not a multi-tenant system needing per-tenant key isolation or a KMS's audit trail, so the added infrastructure wouldn't be earning its keep yet. A compromised `CREDENTIAL_ENCRYPTION_KEY` is the actual incident-scale rotation event (re-encrypt every stored token, force every rep to reconnect) — `security/secrets-rotation-runbook.md` already anticipated this before this decision was made. Alternatives considered: `pgcrypto` (rejected — ties encryption to Postgres specifically instead of keeping it swappable at the application layer, and doesn't actually remove the key-management problem, since Postgres would still need `CREDENTIAL_ENCRYPTION_KEY`'s equivalent available to decrypt on read); a cloud KMS (rejected as premature — its real benefit is centralized key rotation/audit across *many* services, which doesn't exist yet at this scale). Made by Abdoul while building Step 2 (2026-07-12), flagging for Marc's awareness rather than blocking on it — a reversible implementation detail, not a product-shape decision like 026 |
| 030 | ~~Step 2 tool split between Marc and Abdoul~~ — **corrected same day, see Decision 032** | Original proposal: Group A (Abdoul, 5): `fetch_all_leads`, `update_lead_sheet`, `verify_drive_contents`, `fetch_ad_hoc_sheet`, `get_contact_history`. Group B (Marc, 6): `initiate_lead_call`, `log_call_outcome`, `send_lead_text`, `dispatch_slack_handoff`, `send_lead_email`, `search_communications` | Grouped by real dependency, not an even count. Two pairings mattered: `log_call_outcome` + `initiate_lead_call` (row-matching), and `send_lead_email` + `search_communications` (shared Gmail-scope edit to `leadpilot/google_oauth.py`). This version kept both pairs within Group B, so neither dependency crossed the person boundary. Superseded the same day — see Decision 032 for why and what changed |
| 031 | Tool-registration mechanism | `@tool(name, description, input_schema)` decorator per tool module (`leadpilot/tools/<tool_name>.py`), with `pkgutil`-based auto-discovery (`leadpilot/tools/registry.py`) rather than a hand-maintained import list | Built ahead of any actual tool implementation, specifically to support Decision 030's parallel split — a hand-maintained "import every tool module here" list is exactly the kind of shared file two people editing in the same PR window would conflict on. Duplicate tool names raise at import time (loud), not silently shadow each other. Deliberately not wired to the actual Claude Agent SDK yet — that integration is Step 4, and its exact shape depends on that SDK's real tool-calling API; this scaffold only assumes "a tool has a name, description, JSON input schema, and a Python function," which should be stable regardless of how Step 4 ends up calling it |
| 032 | Step 2 tool split — corrected (supersedes Decision 030) | Group A (Abdoul, 5): `fetch_all_leads`, `update_lead_sheet`, `verify_drive_contents`, `fetch_ad_hoc_sheet`, `log_call_outcome`. Group B (Marc, 6): `get_contact_history`, `initiate_lead_call`, `send_lead_email`, `dispatch_slack_handoff`, `search_communications`, `send_lead_text` | Marc separately proposed a split via his own Claude session, without visibility into this session's context, giving himself `fetch_all_leads` through `fetch_ad_hoc_sheet` — unaware the OAuth/connector rework those tools depend on was already built by Abdoul (Decision 026/029). Swapping that group to Abdoul, who already has full working context on that code, avoids Marc re-discovering or duplicating it. Abdoul separately proposed moving `send_lead_text` to himself, reasoning the Twilio trial account's verified-caller-ID restriction meant only his number could be live-tested — that reasoning didn't hold up under a real check: querying the actual Twilio credentials in `.env.local` against Twilio's real API returned a 401 authentication failure, so whose number is even verified was never actually confirmed (root cause not yet resolved with Marc — possibly a rotated Auth Token). `send_lead_text` ended up back with Marc regardless, since it fits his group's theme (outreach/communication) better than Abdoul's (Sheets/Drive). Net effect vs. Decision 030: `get_contact_history` moved to Marc, `log_call_outcome` moved to Abdoul. That last move reintroduces the cross-boundary dependency Decision 030 was designed to avoid — `log_call_outcome` now needs to match a row `initiate_lead_call` (Marc's) creates. Rather than force both tools back onto the same person, the row-matching contract was written down explicitly instead (`architecture/state-schema.md`, "Outcome visibility" section): `log_call_outcome` takes an `event_id` directly (matching `gate.py`'s existing pattern for `approve`/`try_execute`/`reject`), not a `lead_id`-based lookup, and validates `tool=INITIATE_LEAD_CALL`, `stage=EXECUTED`, `outcome=PENDING` before writing — both tools can now be built independently against that written contract. The `send_lead_email`/`search_communications` Gmail-scope pairing from Decision 030 is unaffected and still holds. **Addendum, same day:** Marc's Claude session (reviewing this corrected split independently) caught a stale artifact — `agent_run_locks`' per-rep mutex redesign was still listed under Group B, grouped there under "not tied to a specific tool" reasoning from before Decision 032 existed. It specifically gates `fetch_all_leads`'s per-rep batch loop (per `architecture/state-schema.md`'s own note from when it was first flagged in Step 1), which is Abdoul's tool as of this decision — moved to Group A in `mvp/README.md` |
| 033 | Widen Google scope to `drive.readonly` for `verify_drive_contents` (extends Decision 026) | Added `https://www.googleapis.com/auth/drive.readonly` to the OAuth scope list, alongside the existing `drive.file` (kept for `update_lead_sheet`'s write path and the deliberate per-item consent UX on `fetch_all_leads`/`fetch_ad_hoc_sheet`). `verify_drive_contents` now reads through `drive.readonly`; the product still only *inspects* folders the rep explicitly granted via the Picker (same DATA ACCESS GUARD behavior as before) even though the underlying credential can technically read more | Live-tested and confirmed: granting a folder via the Picker under `drive.file` does not grant visibility into that folder's contents — not a caching/propagation delay, the access token could not see the file at all, anywhere, even via an unfiltered "list everything this token can see" call. Matches Google's own scope documentation ("per-file access") and real developer reports of the same wall. Marc asked whether this was specific to Shared Drive folders (their real production data lives on one) — checked directly: no, the same per-item restriction applies regardless of whether the folder is personal "My Drive" or a Shared Drive; being a Shared Drive doesn't change the scope's behavior. (Separately, confirmed Shared Drive content needs `supportsAllDrives`/`includeItemsFromAllDrives` set on every Drive API call, or the API silently excludes it regardless of scope — an unrelated, additional gotcha, now handled in `GoogleDriveClient`.) Also surfaced and fixed a real bug this widening exposed: `granted_file_ids` is one flat list shared between sheet grants and folder grants, so `fetch_all_leads`'s `list_sources()` was handing folder IDs to the Sheets API and getting a real 400 — `list_sources()` now filters to actual spreadsheets via a Drive mimeType check, while `_sheet_id_for`'s "was this even granted" rejection deliberately still checks the raw list first so genuinely-ungranted IDs are still rejected without a network call. Alternatives considered: keeping `drive.file` only and having each individual document explicitly re-granted by the rep (rejected by Abdoul — defeats the tool's purpose, since a rep would have to manually pick every new document as it shows up, not just point LeadPilot at the folder once). **Flagged to revisit**: `drive.readonly` is a meaningfully bigger blast radius than Decision 026's original "only what's explicitly shared, one item at a time" model — reassess if Google ever ships a narrower scope that actually cascades a folder grant to its children, rather than treating `drive.readonly` as the permanent answer |
| 034 | Same-cell concurrent-write protection for `update_lead_sheet` | `GoogleSheetsConnector.commit_field_write` now requires a new keyword-only `expected_current` argument (the exact `FieldDiff.current` value the rep reviewed when the diff was drafted) and raises `StaleWriteError` if the cell's live value no longer matches it — never silently overwrites. Paired with a new Postgres-backed advisory lock (`sheet_cell_locks` table, `leadpilot.locks.try_acquire_sheet_cell_lock`/`release_sheet_cell_lock`, same `INSERT ... ON CONFLICT` pattern as the existing two locks) keyed to `"{source_id}:{row_ref}:{field_name}"`, serializing concurrent `commit_field_write` calls to the exact same cell rather than letting them race. `ConcurrentWriteError` is the fail-fast case (rare in practice); `StaleWriteError` is what a genuinely concurrent second writer normally gets, since Postgres blocks the second `INSERT ... ON CONFLICT` until the first transaction commits, then the freshness check catches the now-changed value. This is a breaking interface change — `commit_field_write` no longer has a default for `expected_current`, deliberately, so no caller can silently skip the check. | Raised directly by Marc asking what happens if two reps (or two overlapping runs) edit the same spreadsheet cell at once. Investigated the real code: `commit_field_write` was a plain `values().update()` with no compare-and-swap against what the rep actually approved — last-write-wins, no conflict detected, nothing logged. Chose optimistic-value-check-plus-serializing-lock over a pure lock alone because a lock can only stop *LeadPilot-originated* concurrent writes; it can't catch a rep (or anyone) editing the sheet directly in Google's own UI, which never goes through LeadPilot or its lock at all — only re-checking the live value against what was actually approved catches that case too. Alternatives considered: a pure pessimistic per-cell lock with no value check (rejected — doesn't protect against direct edits in Google Sheets' UI, the more likely real-world case for a shared lead spreadsheet); relying on Google Sheets' own revision history to detect conflicts after the fact (rejected — after-the-fact detection means the bad write already happened; the product's core promise is that nothing happens without the rep's approval matching reality, not that conflicts get *noticed* eventually). **Migration/schema-drift caveat, not yet resolved:** the new `sheet_cell_locks` table's migration (`fed4e55c9f58_add_sheet_cell_locks_table.py`) was written with `down_revision = d2caf87d6b35` — the newest migration actually present in the local `leadpilot` checkout this was built against. This decisions log's own "Migration-head correction" note (below) claims a *different*, newer migration already exists upstream (`cd645f125bf4_rework_agent_run_locks_to_per_rep_mutex.py`, Decision 027's per-rep rework) — but that file isn't reachable anywhere in the local checkout's git history, branches, or remote-tracking refs (checked directly via `git rev-list --objects --all` and `git show-ref`; this sandbox can't reach GitHub to fetch and confirm either way). **Before running `alembic upgrade head` on this branch: pull the real latest `main`/`abdouls-branch` from GitHub and run `alembic heads` — if `cd645f125bf4` (or anything newer) turns out to be the real head, edit `fed4e55c9f58`'s `down_revision` to point at it first**, or Alembic will see two divergent heads and need a manual `alembic merge`. Also not yet done: `update_lead_sheet` (Group A, Abdoul's — not present in this local checkout either, see `mvp/README.md`'s note) needs updating to pass `expected_current` and handle both new exceptions by surfacing a fresh diff to the rep, not retrying silently. Not run in this sandbox at all: no Postgres binary and no PyPI access here, so the migration and the new/updated tests (`tests/test_google_sheets_connector.py`, additions to `tests/test_locks.py`, fixes to `tests/test_google_sheets_connector_live.py`) are written but **not yet executed against a real database** — Marc needs to run `alembic upgrade head` then `pytest tests/test_locks.py tests/test_google_sheets_connector.py tests/test_google_sheets_connector_live.py` locally for real evidence before trusting this is correct in practice, not just on paper. |

## Open decisions (not yet made — track here as they resolve)

- Whether `search_communications` (Decision 012) needs a dedicated
  compliance/retention review before use against real client data —
  see testing/known-issues-log.md Issue 004
- Data-modeling decision for a `GoogleDocsConnector` (Decision 015) —
  what structure counts as "one lead record" inside a free-form Doc
  (Docs-native table vs. consistent heading-per-lead vs. something
  else) needs to be settled before that connector can even be
  estimated, let alone built
- Build order/timeline for the remaining planned connectors (Excel via
  Microsoft Graph, OnlyOffice Docs Server, LibreOffice/OpenOffice) —
  none scheduled yet, no validated need beyond Google Sheets so far
- Exact UI placement/flow for `log_call_outcome` (Decision 020) — the
  tool exists in the PRD; the interface design for prompting the rep
  at the right moment isn't done yet
- Secrets management approach (Render env vars assumed by Decision
  022, not yet confirmed) — now needs to additionally cover where each
  rep's OAuth refresh token (Decision 026) is stored/encrypted, not
  just static service secrets
- Exact schema for `agent_run_locks` moving from a singleton mutex to
  a per-rep mutex (Decision 027) — flagged, not designed; Step 2 work
- Google Picker UI integration point in the dashboard (Decision 026) —
  where in the rep-facing interface "Connect Google Account" and
  per-sheet selection live; not designed yet, likely a Step 3 item
  alongside the rest of the unified interface
- Whether Gmail (Step 2, `send_lead_email`) shares the same OAuth
  client/consent screen as the `drive.file` grant (Decision 026) or
  needs its own separate client — not evaluated yet
- Add a dedicated `message_type` column to `contact_history` for
  `dispatch_slack_handoff` (`completion_handoff`/`info_request`/
  `urgent_callback_request`) — `architecture/state-schema.md` has no
  column for this, even though the PRD tracks it as a tracked output
  of the tool. Marc's `leadpilot/tools/dispatch_slack_handoff.py`
  currently stores it in the existing `note` field as a stopgap
  (documented there as "free text," not exclusively for call
  outcomes) rather than unilaterally changing shared schema. Marc
  wants to add the real column but couldn't confirm with Abdoul before
  starting — **needs Abdoul's input on:** whether `note` overloading is
  acceptable short-term or the column should be added now — this part
  is still genuinely open, not resolved here.

  **Migration-head correction (2026-07-12, checked directly against
  the actual `alembic/versions/` files):** the `agent_run_locks`
  per-rep mutex redesign this note flagged as an open risk is already
  done and merged (`abdouls-branch`, Decision 027) —
  `cd645f125bf4_rework_agent_run_locks_to_per_rep_mutex.py`, with
  `down_revision = d2caf87d6b35`. That means the real current head is
  `cd645f125bf4`, not `d2caf87d6b35` as this note originally said —
  any new migration (including the `message_type` one above) needs to
  chain off `cd645f125bf4`, not the older revision, or Alembic will
  see two divergent heads off `d2caf87d6b35` and need a manual
  `alembic merge` to reconcile. Worth pulling `abdouls-branch` (or its
  merged PR) before writing that migration so the real head is visible
  locally.
- Narrower alternative to `drive.readonly` for `verify_drive_contents`
  (Decision 033) — revisit if Google ever ships a scope that lets a
  Picker-granted folder cascade access to its own contents without
  handing over read access to the rep's whole Drive

## Notes

- Write new decisions at the time they are made, not weeks later.
- This log answers "why does it work this way?"
- When a decision is reversed, mark it Superseded and add the new
  decision with a reference back.
- Decisions 001-002 and 006-008 are sourced directly from
  prd/LeadPilot_PRD_v1.md; decisions 009-013 are sourced from
  prd/LeadPilot_PRD_v1.01.md and the raw notes in
  prd/LeadPilot_PRD_v1-changed to make.md; decisions 014-015 are
  sourced from prd/LeadPilot_PRD_v1.02.md; decision 016 is sourced
  from prd/LeadPilot_PRD_v1.03.md; decisions 017-018 were made in
  direct conversation with Marc (2026-07-08) and are now reflected in
  prd/LeadPilot_PRD_v1.04.md; decisions 019-020 are sourced from
  prd/LeadPilot_PRD_v1.04.md; decisions 021-022 are architecture/
  infrastructure decisions, not PRD changes — see
  architecture/state-schema.md and tech-stack/stack-overview.md;
  decisions 023-025 were made by Abdoul while building Step 1
  (2026-07-09/10, `abdouls-branch` in the `leadpilot` code repo,
  merged to `main` by Marc 2026-07-10) —
  023 and 024 are implementation-detail decisions made independently
  to avoid blocking on Step 0; 024 was confirmed by Marc 2026-07-11
  while doing Step 0, then reversed the same day (see 024's entry and
  026 below) once Marc raised how the agent should handle jumping to
  sheets it wasn't pre-configured for; 025 is the dedup/run-lock
  schema design this file previously listed as an open item, since
  partially superseded by 027. Decisions 026-028 came out of that same
  2026-07-11 Step 0 conversation with Claude — see
  `ai-conversations/claude/2026-07-11-step-0-walkthrough-and-sheet-access-model-pivot.md`
  for the full transcript. Decisions 029-032 were made by Abdoul while
  building Step 2 (2026-07-12) — 029 and 031 are implementation-detail
  decisions flagged for Marc's awareness rather than blocking on his
  sign-off, same pattern as 023/024; 030/032 are the work-split agreed
  with Marc directly, documented here rather than only in
  Slack/conversation — 032 supersedes 030 same-day, after Marc's own
  (context-blind) proposal and a Twilio credential check changed the
  actual grouping.
