# AI Conversation — PRD v1.02–v1.04: outreach tools, connector architecture, and closing out Issues 001/003

Date: 2026-07-08
Model: Claude (Sonnet 5, Cowork mode)
Summary: Marc brought Claude the LeadPilot PRD (v1.01) and asked what
it would take to eventually support spreadsheet formats beyond Google
Sheets. That opened into a full working session: Claude found a real
gap in v1.01 (lead-outreach tools were required by the user stories
and system prompt but never defined), fixed it in v1.02 along with a
connector-architecture design, then worked through a chain of
build-readiness questions that each surfaced a further problem —
culminating in discovering Google Voice has no official API and its
Acceptable Use Policy bans automating call/message placement entirely.
That single finding drove most of the rest of the session: redesigning
lead calling as a clipboard handoff (v1.03), retiring the
Google-Voice-dependent back-office call tool in favor of Slack-only
handoffs (v1.04), building a self-maintained contact-history log to
replace the Google-Voice-dependent history tool, and closing out the
rep-approval-token mechanism (Issue 003) with a concrete, crypto-free
design. Governance (equity split) and a compliance gap (rep-approval
≠ legal compliance) were resolved along the way.

Key outputs:
- `prd/LeadPilot_PRD_v1.02.md` — adds `initiate_lead_call`,
  `send_lead_text`, `send_lead_email` (closing the outreach-tool gap)
  and the `LeadSourceConnector` interface (Google Sheets built now;
  Excel/OnlyOffice planned; LibreOffice/OpenOffice/Docs flagged as
  harder)
- `prd/LeadPilot_PRD_v1.03.md` — redefines `initiate_lead_call` as a
  clipboard-copy + confirmation message; no telephony API, no
  automation of Google Voice
- `prd/LeadPilot_PRD_v1.04.md` — retires `initiate_backoffice_call`
  (folded into `dispatch_slack_handoff` as an `urgent_callback_request`
  type, still rep-approved); redefines `get_contact_history` to read
  from a self-maintained log; adds `log_call_outcome`
- `architecture/state-schema.md` — new file: the contact-history log
  schema, and the rep-approval enforcement mechanism (resolves Issue
  003 — no token object, an atomic conditional update on the log row's
  `stage` field)
- Edits to: `decisions/README.md` (Decisions 014–021), `testing/eval-suite.md`
  (Cases 7–10), `testing/known-issues-log.md` (Issues 001 and 003
  resolved), `security/threat-model.md`, `architecture/README.md`,
  `mvp/README.md`, `governance/README.md`, `compliance/README.md`,
  `tech-stack/README.md`

Key decisions (see decisions/README.md #014–021 for the canonical log):
- Lead outreach (call/text/email) follows the same rep-approval gate
  as everything else — no autonomous-send carve-out, even though it
  was tempting to treat outreach differently from spreadsheet writes.
- `initiate_lead_call` never touches Google Voice or any calling app.
  Confirmed directly (web search) that Google Voice has no official
  API and its Acceptable Use Policy explicitly prohibits scripting or
  automating call/message placement — even a script that stops short
  of dialing. Rep approval copies the number to the clipboard instead;
  the rep dials manually.
- `initiate_backoffice_call` is retired — back-office handoffs are
  always a Slack message now, including for what used to be the "call"
  option, and stay rep-approved with no urgency exception.
- `get_contact_history` reads from a LeadPilot-owned, append-only
  contact-history log instead of any external API — this also gave
  Issue 003 (approval mechanism) a home: the same log row's `stage`
  field, gated by an atomic conditional update, is both the audit
  trail and the enforcement mechanism. No signed or opaque token needed.
- `log_call_outcome` exists because calls now happen entirely outside
  LeadPilot — without a rep reporting the outcome back, Rank 3's
  "unanswered call → follow up" rule has no data for calls specifically.
- Google Docs is deferred as a lead-source connector — it doesn't
  share the tabular row/column shape Sheets/Excel/OnlyOffice have in
  common, and needs its own data-modeling decision first.
- Equal 50/50 ownership between Marc and Abdoul; either partner may
  fork or use the work as they see fit (still flagged as needing a
  real written agreement eventually).
- Rep-approval gating reduces bulk/autodialer-style liability but does
  not by itself satisfy TCPA, CAN-SPAM, do-not-call scrubbing, or the
  `search_communications` data-exposure review — those remain open
  compliance items regardless of who clicks approve.

--- transcript below ---

**Marc:** Shared the LeadPilot PRD (raw change notes, then the
authoritative v1.01) and asked what it would take to support
spreadsheet formats beyond Google Sheets, Sheets, and Voice — should
it be designed now or could it wait.

**Claude:** Explained the two integration paradigms (cloud/API-native
like Excel-via-Graph and OnlyOffice, vs. local-file-based like
LibreOffice/OpenOffice with no live API), and recommended designing
the connector *interface* now but building only the Sheets adapter.

**Marc:** Asked Claude to check the authoritative v1.01 PRD against
the raw change notes for conflicts.

**Claude:** Found a real gap: the user stories, section 2, the system
prompt, and Eval Case 1 all required the agent to draft outreach
(call/text/email) to the lead, but the Tools table (3a) had no tool
for it — only back-office-facing tools existed. Flagged this along
with a smaller ambiguity (whether the "before/after diff" requirement
applies only to spreadsheet edits).

**Marc:** Asked Claude to build the missing outreach tool(s) and
separately asked to design the connector architecture, saying "just go
with google docs for now and add the others later" for the connector
target.

**Claude:** Flagged two things before writing anything: "leave the
sending to the agent" directly conflicted with the PRD's core
human-in-the-loop design (0% autonomous-execution success metric,
Eval Cases 5/6), and "google docs" didn't match any tool already in
the PRD (which uses Google *Sheets*). Asked both as explicit
questions rather than guessing.

**Marc (answers):** Keep the rep-approval gate for outreach, no
exception. For the connector, plan for both Sheets and Docs
eventually, but Docs doesn't need to be in the first version unless
it's an easy lift.

**Claude:** Wrote PRD v1.02: three new lead-outreach tools
(`initiate_lead_call`, `send_lead_text`, `send_lead_email`), all
gated the same way as every other side-effect tool; and a
`LeadSourceConnector` interface, with only a `GoogleSheetsConnector`
built, Excel/OnlyOffice flagged as easy future adapters,
LibreOffice/OpenOffice flagged as needing a different polling model,
and Google Docs flagged as *not* an easy lift — it's a free-form
document, not rows/columns, and needs its own data-modeling decision
before it can even be scoped.

**Marc:** Asked what files/folders Claude had access to, then asked
where the NoiseToSignal/CarbonShift context came from (unrelated
imported knowledge that had surfaced automatically) — confirmed those
are separate active projects, not related to LeadPilot.

**Claude:** Explained the file-access model and traced the imported
context to a different Claude.ai project ("Pursuit.org Classwork")
that had nothing to do with LeadPilot.

**Marc:** Asked Claude to write the decisions log entries into the
actual repo, then connected the `leadpilot-docs` folder.

**Claude:** On first connecting, undercounted the repo's contents due
to a truncated directory listing, incorrectly reporting that
`decisions/`, `security/threat-model.md`, and `testing/eval-suite.md`
didn't exist. A full recursive glob corrected this — they did exist.
Read them, then asked how far to propagate the v1.02 changes (just
decisions, or also eval-suite/threat-model/architecture).

**Marc:** All four files.

**Claude:** Added Decisions 014–015 to `decisions/README.md`, Case 7
to `testing/eval-suite.md`, updated `security/threat-model.md`'s
failure-mode safeguard list, and updated `architecture/README.md`'s
tool-map diagram and notes — all now pointing at v1.02.

**Marc:** Asked what else to consider before building.

**Claude:** Surfaced the biggest one: `get_contact_history`,
`initiate_backoffice_call`, and `initiate_lead_call` all assumed a
Google Voice API for calls/texts that (per web search) doesn't
officially exist. Also flagged the unspecified approval-token
mechanism (Issue 003), TCPA/CAN-SPAM compliance gaps introduced by the
new outreach tools, a stale `mvp/README.md`, and the still-undecided
governance/equity split.

**Marc:** Asked specifically whether there's a free, stable way to
queue a call via Google Voice.

**Claude:** Confirmed via search: no. Unofficial libraries and browser
automation both exist but are explicitly fragile and, per Google
Voice's own Acceptable Use Policy, scripting or automating call/message
placement is directly prohibited — enforcement is a two-stage block
escalating to account suspension. Recommended not building on Google
Voice automation at all.

**Marc:** Proposed stopping short of dialing — just pre-fill the
number and stop, leaving the rep to click "Call."

**Claude:** Checked the AUP again: it targets "automating the system,"
which plausibly covers scripted field-filling as part of an automated
call-initiation flow, not just the final click. Recommended against
it — the risk is to the reps' actual phone numbers, not just script
fragility.

**Marc:** Proposed clipping the phone number to the clipboard instead,
with a message that it's ready — never touching Google Voice itself.

**Claude:** Confirmed this sidesteps the problem entirely, since it
never automates Voice's system at all — it's a standard, free
OS/browser clipboard write triggered by the rep's own approval click.
Offered to update the PRD and log it as a decision.

**Marc:** Yes.

**Claude:** Wrote PRD v1.03 — `initiate_lead_call` redefined as a
clipboard-copy + confirmation message, no telephony API at all. Synced
`decisions/README.md` (Decision 016), `testing/known-issues-log.md`
(Issue 001 partially resolved), `testing/eval-suite.md` (Case 8),
`security/threat-model.md`, and `architecture/README.md`.

**Marc:** Asked for options on Issue 003 (approval-token mechanism);
asked whether compliance concerns remain now that the rep approves
everything; asked whether `mvp/README.md` was updated; confirmed
governance ("we equally own it, either of us can fork or use it how we
see fit"); proposed a self-maintained contact-history log instead of
depending on Google Voice, letting Claude choose the file structure;
and proposed retiring the back-office "call" option in favor of a
Slack message, provided the agent can draft it.

**Claude:** Presented three options for Issue 003 (signed token /
status-flip state machine / opaque token), recommending the
status-flip approach. Explained rep-approval doesn't substitute for
TCPA/CAN-SPAM/DNC-list/consent compliance and updated
`compliance/README.md` with what's still outstanding. Held
`mvp/README.md` pending the back-office question, to avoid editing it
twice. Updated `governance/README.md` with the confirmed 50/50 split
and fork rights. Designed and wrote `architecture/state-schema.md` for
the contact-history log, flagging a real gap it introduces: call
outcomes aren't automatically knowable anymore since calls happen
entirely outside LeadPilot, which breaks the Rank 3 unanswered-call
cadence rule until a rep-reported-outcome action exists. Asked one
clarifying question before touching the back-office tool: did "have
the agent send them" mean back-office Slack handoffs should stay
rep-approved, or should the agent send them automatically since
they're internal-only?

**Marc:** Still rep-approved.

**Claude:** Wrote PRD v1.04 — retired `initiate_backoffice_call`
(folded into `dispatch_slack_handoff` as `urgent_callback_request`,
still gated); redefined `get_contact_history` to read from the new
log. While drafting this, Marc separately confirmed: **"yes add the
log call outcome"** — Claude added `log_call_outcome` as a rep-initiated,
ungated tool (writes only to LeadPilot's own log, no external effect)
and a matching Eval Case 10, then propagated all of this across
`decisions/README.md` (017–020), `testing/known-issues-log.md` (Issue
001 fully resolved), `security/threat-model.md`, `architecture/README.md`,
and `mvp/README.md`.

**Marc:** Asked Claude to explain the Issue 003 mechanism in more
depth.

**Claude:** Walked through all three options concretely (what a signed
JWT does step by step, why it adds nothing here since the token never
leaves the backend; the status-flip conditional-update pattern in
detail, including the actual SQL shape; the opaque-token variant and
when it's worth the extra bookkeeping), and reconfirmed the
recommendation.

**Marc:** Asked Claude to write that mechanism into the repo and log
it as a decision, plus anything else that needed updating.

**Claude:** Added the mechanism to `architecture/state-schema.md`
(the contact-history log's own `stage` field *is* the enforcement
point — no separate token table), logged Decision 021, marked Issue
003 resolved in `testing/known-issues-log.md`, and updated
`security/threat-model.md`, `architecture/README.md`, `mvp/README.md`,
and `tech-stack/README.md` (flagging that whichever database gets
chosen needs to support atomic conditional updates, and should be
concurrency-tested once picked).

**Marc:** Asked Claude to save this conversation to
`ai-conversations/claude`, alongside the existing ones.

**Claude:** Wrote this file, following the header/summary/transcript
format defined in `ai-conversations/README.md`.

(This is a condensed reconstruction of the conversation's substance,
not a raw system export — tool-call output, web searches, and
intermediate file reads are summarized rather than pasted verbatim.)
