# AI Conversation — PRD v1.01 revision and repo-wide doc propagation

Date: 2026-07-07
Model: Claude (Sonnet 5, Claude Code)
Summary: Abdoul asked Claude to turn the raw change notes in
`prd/LeadPilot_PRD_v1-changed to make.md` into a new, versioned PRD
(`LeadPilot_PRD_v1.01.md`) without editing the original v1 PRD, then to
propagate every resulting change across the rest of the docs repo.
Claude first read both PRD files and the surrounding docs (context
files, threat model, eval suite, MVP checklist, decisions log) to scope
the work, then asked four clarifying questions on ambiguous points
before writing anything. After getting answers, it wrote the new PRD
and updated 15 other files across 10 folders to keep them consistent
with it.

Key outputs:
- `prd/LeadPilot_PRD_v1.01.md` — new PRD (v1 untouched, kept as the
  historical starting point)
- `context-files/leadpilot_context-v002.md` — new versioned context
  file (v001 kept untouched)
- Edits to: `prd/README.md`, `context-files/README.md`,
  `security/threat-model.md`, `security/pen-test-checklist.md`,
  `testing/eval-suite.md`, `testing/ci-strategy.md`,
  `testing/definition-of-done.md`, `testing/known-issues-log.md`,
  `mvp/README.md`, `decisions/README.md`, `architecture/README.md`,
  `roadmap/README.md`, `settings/README.md`, `compliance/README.md`,
  `commands/README.md`

Key decisions (see decisions/README.md #009–013 for the canonical log):
- Nothing fires automatically anymore — the agent only drafts
  (outreach, back-office handoff, spreadsheet edits); every side-effect
  action requires explicit rep approval via a single-use approval
  token. This resolves the "auto-send vs. rep approval" item that was
  open since v1.
- The back-office handoff can now be either a phone call
  (`initiate_backoffice_call`, new tool) or a predetermined Slack
  message (`dispatch_slack_handoff`, extended) — both staged, both
  gated on rep approval.
- Added `update_lead_sheet` (write-back to source sheets from the
  unified interface, gated on a shown current-vs-proposed diff) and
  `search_communications` (search email/text history by any known
  identifier — email, phone, name, company) as new tools, per Abdoul's
  guidance to extend existing tools where scope allows and only add
  new ones when the scope genuinely changes.
- Access control resolved as: only authenticated, pre-authorized rep
  accounts may trigger the agent, view lead data, or approve staged
  actions.

--- transcript below ---

**Abdoul:** Okay look at my project. Look in my prd folder, i have
LeadPilot_PRD_V1.md and LeadPilot_PRD_V1-changed to make.md.
LeadPilot_PRD_V1.md is the first version of me and partner's prd. i
need you to look at the changes that we said we needed to make. Those
changes are listed in LeadPilot_PRD_V1-changed to make.md. I need you
to make another prd, you can call it LeadPilot_PRD_V1.01.md or
something like that. Make that file and make those changes to that
document and put it into that file. Don't edit the original prd files
though. Then we'll be using that prd moving forward. Then, look
through the entire project, every folder, and change everything you
need to change to tailor it to the new prd. Do you understand the
task? Ask me any clarifying questions before you continue. And let me
know all the changes you make in each folder

**Claude:** Read both PRD files and the rest of the repo (all README
scaffolding, context files, threat model, eval suite, MVP checklist,
decisions log, architecture doc, pen-test checklist) to scope what
"tailor everything" would actually touch. Confirmed this is a
docs-only repo — mostly scaffolded READMEs plus a well-developed
security/testing/PRD set that explicitly cross-references the PRD as
source of truth.

Identified four points in the change notes that were genuinely
ambiguous and asked before writing anything:

1. How far "rep must approve before execution" extends — does it cover
   the internal back-office Slack handoff too, or just lead-facing
   outreach?
2. The change notes had an incomplete sentence — "the handoff can be a
   call or a can be a predetermined message to all three stakeholders
   via slack including." — what was meant?
3. For the new capabilities (spreadsheet write-back, communications
   search, back-office info requests), should these be new dedicated
   tools or extensions of the existing four?
4. What "lock down use of the agent by either the account using the
   agent or some other method" meant concretely.

**Abdoul (answers):**
1. "the agent sets up the appropriate outreach and everything else and
   gives suggestions but gives it back for rep review. Don't fire
   automatically." → everything requires approval, no exceptions.
2. Confirmed: handoff = phone call OR Slack message.
3. "if it fits in an existing tool, then extend it. But, if you have
   to change the scope and what the existing tool actually does, add a
   new dedicated tool."
4. "users who are logged in/authenticated and are using the agent can
   lock down their use of the agent" → authenticated-session access
   control.

**Claude:** Applied those answers: `update_lead_sheet` and
`search_communications` became new tools (different API surface than
anything existing); the back-office info-request use case got folded
into an extended `dispatch_slack_handoff`; the call-based handoff
became a new tool (`initiate_backoffice_call`) since it's a different
API family (Voice, not Slack) than the existing handoff tool.

Wrote `prd/LeadPilot_PRD_v1.01.md` with: three new user stories under
1b, an execution-gating rule under 3a, an updated system prompt (3b)
that stages every side-effect action instead of auto-firing any of
them, two new blast-radius failure modes (3c) — autonomous execution
bypass, unauthorized agent access — and three new eval cases (3d,
Cases 4–6) for communications search, destructive-action confirmation,
and unauthorized access.

Then grepped the repo for every reference to the four original tool
names, "auto-sent"/"auto-send", and "3 stakeholders" to find every file
that needed to change in step with the new PRD, and propagated the
changes folder by folder: `prd/README.md` and `context-files/README.md`
pointers; a new `context-files/leadpilot_context-v002.md`;
`security/threat-model.md` and `pen-test-checklist.md` with the two new
failure modes and matching checklist items; `testing/eval-suite.md`,
`ci-strategy.md`, `definition-of-done.md`, and `known-issues-log.md`
(resolved Issue 002, the open auto-send question, and flagged two new
issues — the approval-token mechanism and a compliance review needed
for `search_communications`); `mvp/README.md`'s Phase 1 checklist;
`decisions/README.md` (five new decision entries, 009–013);
`architecture/README.md`'s tool-map diagram; `roadmap/README.md`'s
public-facing language; and `settings/README.md`, `compliance/README.md`,
`commands/README.md` for the new configurable values, compliance
considerations, and env vars the new tools introduce.

Confirmed via `git status` at the end that both original PRD files
(`LeadPilot_PRD_v1.md` and `LeadPilot_PRD_v1-changed to make.md`) were
never modified.

**Abdoul:** can you also record this conversation and put it into
ai-conversations/claude

**Claude:** Wrote this file, following the header/summary/transcript
format defined in `ai-conversations/README.md`.

(This is a condensed reconstruction of the conversation's substance,
not a raw system export — tool-call output and intermediate file reads
are summarized rather than pasted verbatim.)
